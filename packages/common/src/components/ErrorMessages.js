import React from 'react';
import { FontAwesomeIcon } from '../../utils/FontAwesome';
import { faEmergency }
import { colors } from '../constants';

export const browserErrorContent = () => {
    return (
        <FontAwesomeIcon 
    )
}